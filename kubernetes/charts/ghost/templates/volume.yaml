---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ghost-content-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage
